package user

// User is a minimal struct to allow generated stubs to compile.
// Fields align with DSL keys used in examples.
type User struct {
	Name   string `json:"name"`
	Active bool   `json:"active"`
}
